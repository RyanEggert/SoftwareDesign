# esca.py

The final code can be found in the "escapy" folder. API keys for [Google QPX Express API](https://developers.google.com/qpx-express/) and [forecast.io](https://developer.forecast.io/) are neccessary to run this program.
Optional is an API key<sup>*</sup> to use the [GoogleV3 geocoder API](https://developers.google.com/maps/documentation/geocoding/). Scripts outside of the "escapy" folder were used for testing and experimentation purposes.


<sup>*</sup>Because Google QPX Express and the Google geocoder are both operated by Google, the same API key can work for both. Howevever, access to both must be enabled from within the "Google Developers Console".
